"use client";import Link from "next/link";import {Download,Printer,ArrowLeft} from "lucide-react";export function ReceiptControls({receiptId,serviceId,receiptNumber}:{receiptId:string;serviceId:string;receiptNumber:string}){return <div className="receiptControls noPrint"><Link className="btn secondary" href={`/servicos/${serviceId}`}><ArrowLeft size={16}/> Voltar para OS</Link><a className="btn secondary" href={`/api/recibos/${receiptId}/pdf`} download={`${receiptNumber}.pdf`}><Download size={16}/> Baixar PDF</a><button className="btn" onClick={()=>window.print()}><Printer size={16}/> Imprimir</button></div>}